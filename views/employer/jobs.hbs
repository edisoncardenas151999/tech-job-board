<title>Jobs</title>
<ul class="list-wrapper">
    {{#if user.jobs}}
    {{#each user.jobs}}
   <div class="job-detail-container">
    <div class="job-info-header">
        <h3 class="job-title">{{jobTitle}}</h3>
        <h4 class="job-company">{{company}}</h4>
        <p class="job-location">{{location}}</p>
    </div>
    <div class="job-info-details">
        <p class="job-salary">${{salary}} annually</p>
        <p class="job-description">{{description}}</p>
    </div>
      <a href="/employer/{{_id}}/edit">Edit</a>
        <br>
        <a href="/employer/{{_id}}/applicants">View Applicants</a>

</div>
    {{/each}}
    {{else}}

    <div class="submission-container">
        <h2 class="application-message">No jobs Found, Create New Job Post</h2>
    </div>
    {{/if}}
</ul>
    <br>
    <a href="dashboard" class="back-link">back</a>

  
{{!-- <ul class="list-wrapper">
    {{#if jobs}}
    <p>All results</p>
    {{#each jobs}}
    <li class="company-info">
        <h3 class="list-company-job-title">{{this.jobTitle}}</h3>
        <h4 class="list-company-name">{{this.company}}</h4>
        <p class="list-company-location">{{this.location}}</p>
        <a href="/jobs/{{this._id}}" class="job-details-btn">See description</a>

    </li>
    {{/each}}
    {{else}}
    <p id="no-match-found">No match found</p>
    {{/if}}

    <a href="/" class="back-link">back</a>
</ul> --}}